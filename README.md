# nccvector.github.io

Faizan Ali's single-page robotics portfolio. React source lives in `src/` and
the production build is written directly to the repository root for GitHub Pages.

```bash
npm install
npm run dev
npm run build
```

`npm run build` also regenerates the deterministic project thumbnails from
`scripts/render-project-thumbnails.mjs`.
